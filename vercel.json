{
  "buildCommand": "npm run build || true",
  "devCommand": "npm run dev",
  "installCommand": "npm ci",
  "framework": "express",
  "regions": ["sin1"],
  "routes": [
    {
      "src": "^/api/.*",
      "dest": "/api/index.js"
    }
  ]
}

