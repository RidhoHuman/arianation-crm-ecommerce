{
  "version": 2,
  "installCommand": "npm ci && cd frontend && npm ci",
  "buildCommand": "npm run setup:db && cd frontend && npm run build",
  "devCommand": "npm run dev",
  "regions": ["sin1"],
  "routes": [
    {
      "src": "^/api/.*",
      "dest": "/api/index.js"
    },
    {
      "src": "^/(.*)$",
      "dest": "/api/index.js"
    }
  ]
}

